pipeline {
  agent { label 'raspi-agent' } 
  stages {
    stage('Serve') {
      steps {
        // Arranca serve en background; asume que 'serve' ya está instalado en el agente
        echo 'Hola'
      }
    }
  }
}
